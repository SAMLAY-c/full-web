version: '3.8'

services:
  sanity-studio:
    build:
      context: .
      dockerfile: apps/studio/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
    # 如果需要限制资源
    # deploy:
    #   resources:
    #     limits:
    #       memory: 512M
    #     reservations:
    #       memory: 256M
